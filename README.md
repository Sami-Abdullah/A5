 
  - 1️⃣ What is the difference between var, let, and const?
  - 2️⃣ What is the spread operator (...)?
  - 3️⃣ What is the difference between map(), filter(), and forEach()?
  - 4️⃣ What is an arrow function?
  - 5️⃣ What are template literals?

1. var let const these are variable delaration key words not data types. The difference between these are few, but the main aspects are scope and reassignment. var and let can be reassigned but const cant be reassigned, var is function scoped and let and const are blocked scoped

2. It is a operator in js used to spread out or expand the elements of array or object


3. All the mentioned functions are array fucntion, map performs some operation per instruction and return a new array, same for fileter this function filter the array per condition and returns a new arry of filter objects. foreach is a fubnction use for traverse the array one by one

4. Arrow function is from the ES6 module works just like a function with a difference in "this", arrow does not have its own "this" but function has 

5. Template literals are also part of the new ES6 features; it makes change of HTML of a page more conveniently and dynamically.